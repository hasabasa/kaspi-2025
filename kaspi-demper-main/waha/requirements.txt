# requirements.txt для WAHA модуля
# Дополнительные зависимости для WAHA интеграции

# Основные зависимости (уже должны быть в основном проекте)
fastapi>=0.104.0
uvicorn>=0.24.0
asyncpg>=0.29.0
aiohttp>=3.9.0
pydantic>=2.5.0

# Дополнительные зависимости для WAHA
httpx>=0.25.0  # Для HTTP клиента
python-multipart>=0.0.6  # Для обработки форм
python-dateutil>=2.8.2  # Для работы с датами
pytz>=2023.3  # Для работы с часовыми поясами

# Зависимости для логирования и мониторинга
structlog>=23.2.0  # Структурированное логирование
prometheus-client>=0.19.0  # Метрики (опционально)

# Зависимости для тестирования (опционально)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0  # Для тестирования HTTP клиентов
