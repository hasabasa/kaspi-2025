# 🚀 Инструкция по подключению WhatsApp номера

## ✅ Результаты тестирования

Все тесты WAHA интеграции **пройдены успешно**! 

### 📊 Что протестировано:
- ✅ Создание и управление сессиями WAHA
- ✅ Генерация QR кодов для подключения
- ✅ Отправка сообщений через WhatsApp
- ✅ Обработка шаблонов сообщений
- ✅ Полный рабочий процесс уведомлений

### 📱 Пример готового сообщения:
```
Здравствуйте, Айдар Нурланов.
Ваш заказ Nº ORD-12345 "iPhone 15 Pro", количество: 1 шт готов к самовывозу.

* В ближайшее время мы свяжемся с вами для уточнения деталей заказа.
* Спасибо за Ваш выбор! Если у Вас есть вопросы, обращайтесь в любое время.

С уважением,
TechStore Kazakhstan
```

## 🛠 Установка и настройка

### 1. Установка Docker

#### Для macOS:
```bash
# Установка через Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install --cask docker

# Или скачайте Docker Desktop с официального сайта:
# https://docs.docker.com/desktop/mac/install/
```

#### Для Linux (Ubuntu/Debian):
```bash
# Обновление пакетов
sudo apt update

# Установка Docker
sudo apt install docker.io docker-compose

# Запуск Docker
sudo systemctl start docker
sudo systemctl enable docker
```

#### Для Windows:
1. Скачайте Docker Desktop с https://docs.docker.com/desktop/windows/install/
2. Установите и перезагрузите компьютер
3. Запустите Docker Desktop

### 2. Запуск WAHA сервера

```bash
# Перейдите в папку WAHA
cd /Users/hasen/demper-667-45/kaspi-demper-main/waha

# Запустите WAHA сервер
docker-compose -f docker-compose.waha.yml up -d

# Проверьте статус
docker-compose -f docker-compose.waha.yml ps
```

### 3. Подключение номера WhatsApp

#### Вариант 1: Через QR код (рекомендуется)
```bash
# Получите QR код
curl http://localhost:3000/api/sessions/kaspi_demper_session/qr

# Отсканируйте QR код в WhatsApp:
# WhatsApp → Настройки → Связанные устройства → Связать устройство
```

#### Вариант 2: Через связанные устройства
1. Откройте WhatsApp на телефоне
2. Перейдите в **Настройки → Связанные устройства**
3. Нажмите **"Связать устройство"**
4. Отсканируйте QR код с экрана

### 4. Проверка подключения

```bash
# Проверьте статус сессии
curl http://localhost:3000/api/sessions/kaspi_demper_session/status

# Ожидаемый ответ:
{
  "status": "CONNECTED",
  "qr": null
}
```

### 5. Тестовая отправка сообщения

```bash
# Отправьте тестовое сообщение
curl -X POST http://localhost:3000/api/sendText \
  -H "Content-Type: application/json" \
  -d '{
    "session": "kaspi_demper_session",
    "chatId": "77001234567@c.us",
    "text": "Тестовое сообщение от Kaspi Demper! 🚀"
  }'
```

## 🔧 Интеграция с Kaspi Demper

### 1. Добавление в main.py

```python
# В файле kaspi-demper-main/backend/main.py
from waha import WAHAIntegration

# Инициализация WAHA
waha_integration = WAHAIntegration()

@app.on_event("startup")
async def startup_event():
    await waha_integration.initialize()

@app.on_event("shutdown")
async def shutdown_event():
    await waha_integration.cleanup()
```

### 2. Модификация api_parser.py

```python
# В файле kaspi-demper-main/backend/api_parser.py
from waha import WAHAIntegration

async def get_sells(shop_id):
    # ... существующий код ...
    
    # Отправка уведомлений через WAHA
    if waha_integration.is_ready():
        await waha_integration.process_new_orders(orders_data)
    
    return result
```

## 📋 Мониторинг и логи

### Просмотр логов WAHA:
```bash
# Логи контейнера
docker-compose -f docker-compose.waha.yml logs -f

# Статус контейнеров
docker-compose -f docker-compose.waha.yml ps
```

### Проверка отправленных сообщений:
```bash
# Получить все сообщения
curl http://localhost:3000/api/sessions/kaspi_demper_session/messages
```

## 🚨 Устранение неполадок

### Проблема: WAHA сервер не запускается
```bash
# Проверьте Docker
docker --version
docker-compose --version

# Перезапустите сервисы
docker-compose -f docker-compose.waha.yml down
docker-compose -f docker-compose.waha.yml up -d
```

### Проблема: QR код не генерируется
```bash
# Пересоздайте сессию
curl -X DELETE http://localhost:3000/api/sessions/kaspi_demper_session
curl -X POST http://localhost:3000/api/sessions/start \
  -H "Content-Type: application/json" \
  -d '{"name": "kaspi_demper_session"}'
```

### Проблема: Сообщения не отправляются
1. Проверьте статус сессии: `CONNECTED`
2. Убедитесь, что номер в формате: `77001234567@c.us`
3. Проверьте логи на ошибки

## 🎯 Следующие шаги

1. **Установите Docker** на вашем сервере
2. **Запустите WAHA сервер** через docker-compose
3. **Подключите номер** через QR код
4. **Протестируйте отправку** сообщений
5. **Интегрируйте** с основным приложением Kaspi Demper

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь в доступности порта 3000
3. Проверьте подключение к интернету
4. Перезапустите WAHA сервер

**WAHA интеграция готова к использованию!** 🎉
