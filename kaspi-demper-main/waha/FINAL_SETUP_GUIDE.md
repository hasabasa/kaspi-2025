# FINAL_SETUP_GUIDE.md
# 🎉 Финальное руководство по настройке WAHA интеграции

## ✅ Что было создано

Полная WAHA интеграция для Kaspi Demper включает в себя:

### 📁 Структура файлов:
```
/Users/hasen/demper-667-45/kaspi-demper-main/waha/
├── __init__.py                    ✅ Инициализация модуля
├── waha_client.py                 ✅ Клиент для WAHA API
├── models.py                      ✅ Pydantic модели
├── template_manager.py            ✅ Управление шаблонами
├── message_sender.py              ✅ Отправка сообщений
├── order_integration.py           ✅ Интеграция с заказами
├── database.py                    ✅ Работа с БД
├── routes.py                      ✅ API эндпоинты
├── waha_integration.py            ✅ Главный менеджер
├── config.py                      ✅ Конфигурация
├── utils.py                       ✅ Утилиты
├── monitoring.py                  ✅ Мониторинг
├── security.py                    ✅ Безопасность
├── docker-compose.waha.yml        ✅ Docker для WAHA
├── requirements.txt               ✅ Зависимости
├── test_waha.py                   ✅ Тесты
├── integration_example.py         ✅ Пример интеграции
├── complete_integration_example.py ✅ Полная интеграция
├── README.md                      ✅ Документация
├── DEPLOYMENT.md                  ✅ Инструкции по развертыванию
└── FINAL_SETUP_GUIDE.md           ✅ Это руководство
```

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Запуск WAHA сервера
```bash
cd /Users/hasen/demper-667-45/kaspi-demper-main/waha/
docker-compose -f docker-compose.waha.yml up -d
```

### Шаг 2: Интеграция в main.py
Добавьте в ваш `main.py`:
```python
from waha.waha_integration import initialize_waha, get_waha_router, shutdown_waha

@app.on_event("startup")
async def startup_event():
    await initialize_waha(pool, "http://localhost:3000")

app.include_router(get_waha_router())

@app.on_event("shutdown")
async def shutdown_event():
    await shutdown_waha()
```

### Шаг 3: Модификация api_parser.py
Добавьте в функцию `get_sells()`:
```python
from waha.waha_integration import get_waha_manager

async def get_sells(shop_id):
    # ... существующий код ...
    
    # WAHA интеграция
    waha_manager = get_waha_manager()
    await waha_manager.process_orders_for_store(
        shop_id, result.get('orders', []), "Магазин"
    )
    
    return True, result
```

### Шаг 4: Настройка магазина
```bash
# Создание настроек
curl -X POST "http://localhost:8000/waha/settings/YOUR_STORE_ID" \
  -H "Content-Type: application/json" \
  -d '{
    "waha_server_url": "http://localhost:3000",
    "waha_session_name": "kaspi-session",
    "is_enabled": true,
    "webhook_url": "http://your-server.com/webhook/waha"
  }'

# Подключение WhatsApp
curl -X POST "http://localhost:8000/waha/sessions/connect/YOUR_STORE_ID" \
  -H "Content-Type: application/json" \
  -d '{"webhook_url": "http://your-server.com/webhook/waha"}'

# Создание шаблона
curl -X POST "http://localhost:8000/waha/templates/YOUR_STORE_ID" \
  -H "Content-Type: application/json" \
  -d '{
    "template_name": "Уведомление о заказе",
    "template_text": "Здравствуйте, {user_name}! Ваш заказ Nº {order_num} готов к самовывозу. С уважением, {shop_name}"
  }'
```

### Шаг 5: Подключение WhatsApp
1. Откройте WhatsApp на телефоне
2. Настройки → Связанные устройства
3. Связать устройство
4. Отсканируйте QR-код с http://localhost:3000

## 🔧 Детальная настройка

### Конфигурация окружения
Создайте файл `.env`:
```env
# WAHA Configuration
WAHA_SERVER_URL=http://localhost:3000
WAHA_WEBHOOK_BASE_URL=http://your-server.com
WAHA_MAX_MESSAGES_PER_DAY=1000
WAHA_MESSAGE_DELAY_SECONDS=1.0
WAHA_RETRY_ATTEMPTS=3
WAHA_SESSION_TIMEOUT_MINUTES=30

# Security
WAHA_ENABLE_RATE_LIMITING=true
WAHA_RATE_LIMIT_WINDOW_MINUTES=60
WAHA_MAX_MESSAGES_PER_WINDOW=100
WAHA_VALIDATE_PHONE_NUMBERS=true

# Monitoring
WAHA_ENABLE_METRICS=true
WAHA_LOG_LEVEL=INFO
WAHA_ENABLE_ADMIN_NOTIFICATIONS=true
WAHA_ADMIN_NOTIFICATION_THRESHOLD=10
```

### Полная интеграция с мониторингом
```python
# В main.py
from waha.monitoring import initialize_monitoring, shutdown_monitoring

@app.on_event("startup")
async def startup_event():
    # ... существующий код ...
    
    # Инициализация WAHA
    waha_manager = await initialize_waha(pool, "http://localhost:3000")
    
    # Инициализация мониторинга
    await initialize_monitoring(waha_manager.waha_db)

@app.on_event("shutdown")
async def shutdown_event():
    await shutdown_monitoring()
    await shutdown_waha()
```

## 📊 API Эндпоинты

### Управление сессиями:
- `POST /waha/sessions/connect/{store_id}` - Подключение сессии
- `GET /waha/sessions/status/{store_id}` - Статус сессии
- `POST /waha/sessions/restart/{store_id}` - Перезапуск сессии
- `POST /waha/sessions/stop/{store_id}` - Остановка сессии

### Управление шаблонами:
- `POST /waha/templates/{store_id}` - Создание шаблона
- `GET /waha/templates/{store_id}` - Список шаблонов
- `PUT /waha/templates/{template_id}` - Обновление шаблона
- `DELETE /waha/templates/{template_id}` - Удаление шаблона
- `POST /waha/templates/preview` - Предварительный просмотр

### Настройки:
- `POST /waha/settings/{store_id}` - Создание/обновление настроек
- `GET /waha/settings/{store_id}` - Получение настроек

### Отправка сообщений:
- `POST /waha/send-test/{store_id}` - Тестовое сообщение
- `POST /waha/check-phone/{store_id}` - Проверка номера телефона

### Статистика и мониторинг:
- `GET /waha/statistics/{store_id}` - Статистика сообщений
- `GET /waha/logs/{store_id}` - Логи сообщений
- `GET /waha/health` - Проверка состояния системы

### Webhook:
- `POST /waha/webhook` - Обработка webhook событий

## 🔒 Безопасность

### Валидация данных:
- Автоматическая очистка входных данных
- Валидация номеров телефонов
- Проверка шаблонов на XSS и инъекции
- Ограничение скорости отправки сообщений

### Контроль доступа:
- API ключи для магазинов
- Токены сессий с истечением
- Ограничение по IP адресам
- Аудит безопасности

### Мониторинг безопасности:
- Логирование всех событий безопасности
- Алерты при подозрительной активности
- Отчеты по безопасности
- Автоматическая блокировка при превышении лимитов

## 📈 Мониторинг и метрики

### Автоматический мониторинг:
- Проверка здоровья WAHA сервера
- Мониторинг активных сессий
- Отслеживание ошибок отправки
- Статистика доставки сообщений

### Метрики:
- Количество отправленных сообщений
- Процент успешной доставки
- Время отклика WAHA API
- Статистика по магазинам

### Алерты:
- WAHA сервер недоступен
- Высокий уровень ошибок
- Отключенные сессии
- Превышение лимитов

## 🧪 Тестирование

### Запуск тестов:
```bash
cd waha/
python -m pytest test_waha.py -v
```

### Тестирование API:
```bash
# Проверка здоровья
curl http://localhost:8000/waha/health

# Тестовое сообщение
curl -X POST "http://localhost:8000/waha/send-test/YOUR_STORE_ID" \
  -H "Content-Type: application/json" \
  -d '{
    "phone_number": "+7XXXXXXXXXX",
    "template_text": "Тест для {user_name}",
    "sample_data": {"user_name": "Тест"}
  }'
```

## 🐳 Docker развертывание

### Полная система:
```bash
# Запуск всех сервисов
docker-compose -f docker-compose.full.yml up -d

# Проверка статуса
docker-compose -f docker-compose.full.yml ps

# Логи
docker-compose -f docker-compose.full.yml logs -f
```

### Отдельные сервисы:
```bash
# Только WAHA
docker-compose -f docker-compose.waha.yml up -d

# Основное приложение
docker build -t kaspi-demper .
docker run -p 8000:8000 kaspi-demper
```

## 🔧 Troubleshooting

### Проблемы с WAHA сервером:
```bash
# Проверка статуса
docker ps | grep waha

# Перезапуск
docker-compose -f docker-compose.waha.yml restart

# Логи
docker logs kaspi-waha -f
```

### Проблемы с сессиями:
1. Проверьте статус: `GET /waha/sessions/status/{store_id}`
2. Перезапустите сессию: `POST /waha/sessions/restart/{store_id}`
3. Проверьте QR-код: http://localhost:3000

### Проблемы с отправкой:
1. Проверьте статус сессии
2. Валидируйте номер телефона
3. Проверьте логи в БД: таблица `whatsapp_messages_log`

### Проблемы с webhook:
1. Убедитесь, что URL доступен извне
2. Проверьте настройки webhook в WAHA
3. Проверьте логи обработки webhook

## 📚 Дополнительные ресурсы

### Документация:
- [WAHA Official Docs](https://waha.devlike.pro/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

### Полезные ссылки:
- API документация: http://localhost:8000/docs
- WAHA Dashboard: http://localhost:3000
- Swagger UI: http://localhost:8000/docs
- Health Check: http://localhost:8000/health

## 🎯 Следующие шаги

### После настройки:
1. ✅ Настройте все магазины через API
2. ✅ Подключите WhatsApp для каждого магазина
3. ✅ Создайте кастомные шаблоны сообщений
4. ✅ Настройте мониторинг и алерты
5. ✅ Протестируйте отправку уведомлений
6. ✅ Настройте резервное копирование
7. ✅ Обучите команду работе с системой

### Оптимизация:
- Настройте автоматическое масштабирование
- Добавьте кеширование для улучшения производительности
- Настройте логирование в централизованную систему
- Добавьте дополнительные метрики для бизнес-аналитики

## 🎉 Готово!

WAHA интеграция полностью настроена и готова к использованию!

**Основные возможности:**
- ✅ Автоматическая отправка WhatsApp уведомлений о заказах
- ✅ Подключение через связанные устройства (без QR-кода)
- ✅ Кастомные шаблоны сообщений с переменными
- ✅ Webhook для входящих сообщений и статусов
- ✅ Полный REST API для управления
- ✅ Мониторинг и метрики
- ✅ Безопасность и валидация
- ✅ Масштабируемость для множественных магазинов

**Система готова к продакшену!** 🚀
